* Generated by open-abap-gui converter 0.1.0.
* Source object: ZEXAMPLE_NAVIGATION
* Source file: input/zexample_navigation.prog.abap
* Source SHA-256: c4ad52602b7f944bb95bc1a9671307388e69cc9def5dcd4a69d40c84ff563d86
* Target class: ZCL_EXAMPLE_NAVIGATION
* Manifest schema: 1
* This file is generated migration output; it is safe to edit after review.
CLASS zcl_example_navigation DEFINITION PUBLIC FINAL CREATE PUBLIC.

  PUBLIC SECTION.
    INTERFACES zif_gg_report_v1.
    INTERFACES zif_gg_screen_provider_v1.
    INTERFACES zif_gg_transaction_v1.
    INTERFACES zif_gg_list_processing_v1.
    INTERFACES zif_gg_resumable_v1.

  PRIVATE SECTION.
    DATA gv_step TYPE i.
    DATA mv_p_target TYPE string.

ENDCLASS.

CLASS zcl_example_navigation IMPLEMENTATION.

  METHOD zif_gg_transaction_v1~get_transaction.
    rs_transaction = VALUE #( tcode = 'ZEXAMPLE_NAVIGATION' description = 'Converted executable report' program = 'ZEXAMPLE_NAVIGATION' ).
  ENDMETHOD.

  METHOD zif_gg_report_v1~load_of_program.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_report_v1~get_logical_database.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_report_v1~get_list_processing.
    ro_list_processing = me.
  ENDMETHOD.

  METHOD zif_gg_report_v1~build_screen.
    io_builder->add_parameter( VALUE #( name = 'P_TARGET' text = 'P_TARGET' data_type = VALUE #( typ = 'C' length = 1 ) default = 'S' ) ).
  ENDMETHOD.

  METHOD zif_gg_report_v1~initialization.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_output.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_on_field.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_on_end_of.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_on_block.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_on_radio.
    mv_p_target = ct_values[ name = 'P_TARGET' ]-value.
    ct_values[ name = 'P_TARGET' ]-value = mv_p_target.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_value_req.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_help_req.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_selection_screen_on_exit.
    mv_p_target = it_values[ name = 'P_TARGET' ]-value.
  ENDMETHOD.

  METHOD zif_gg_report_v1~start_of_selection.
    io_session->get_list( )->set_title( 'ZCL_EXAMPLE_NAVIGATION' ).
    DATA(lo_writer) = io_session->get_list( )->get_writer( ).
    mv_p_target = it_values[ name = 'P_TARGET' ]-value.
    gv_step = 1.
    lo_writer->write_field( VALUE #( text = 'Before navigation, step' placement = VALUE #( new_line = abap_true ) ) ).
    lo_writer->write_field( VALUE #( text = |{ gv_step }| ) ).
    CASE mv_p_target.
      WHEN 'S'.
        io_session->get_dialog( )->call_screen(
          is_call         = VALUE #( screen = '0100' )
          is_continuation = VALUE #( id = 'AFTER_0100' ) ).
    ENDCASE.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_get.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_report_v1~at_get_late.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_report_v1~end_of_selection.
    mv_p_target = it_values[ name = 'P_TARGET' ]-value.
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~get_initial_screen.
    rv_screen = '0100'.
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~build_screens.
    io_builder->begin_screen( VALUE #( number = '0100' title = 'Intermediate screen' next_screen = '0100' width = 600 height = 280 ) ).
    io_builder->add_text( VALUE #( control = VALUE #( name = 'LABEL_INFO' position = VALUE #( row = 36 column = 15 width = 330 height = 26 ) ) text = 'Press Back to continue the report' ) ).
    io_builder->end_screen( ).
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~build_flow_logic.
    io_builder->begin_screen( '0100' ).
    io_builder->begin_pbo( ).
    io_builder->add_module( VALUE #( name = 'STATUS_0100' ) ).
    io_builder->end_processing( ).
    io_builder->begin_pai( ).
    io_builder->add_module( VALUE #( name = 'USER_COMMAND_0100' on_input = abap_true ) ).
    io_builder->end_processing( ).
    io_builder->end_screen( ).
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~initialization.
    IF line_exists( ct_values[ name = 'GV_STEP' ] ).
      ct_values[ name = 'GV_STEP' ]-value = CONV string( gv_step ).
    ELSE.
      INSERT VALUE #( name = 'GV_STEP' value = CONV string( gv_step ) ) INTO TABLE ct_values.
    ENDIF.
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~process_output_module.
    IF line_exists( ct_values[ name = 'GV_STEP' ] ).
      gv_step = CONV #( ct_values[ name = 'GV_STEP' ]-value ).
    ENDIF.
    IF is_context-screen = '0100'.
      io_session->get_dialog( )->set_status( VALUE #( status = 'SCREEN' ) ).
    ENDIF.
    CASE is_context-module.
      WHEN 'STATUS_0100'.
        io_session->get_dialog( )->set_status( VALUE #( status = 'SCREEN' active_ucomm = VALUE #( ( 'BACK' ) ) ) ).
      WHEN OTHERS.
        RETURN.
    ENDCASE.
    IF line_exists( ct_values[ name = 'GV_STEP' ] ).
      ct_values[ name = 'GV_STEP' ]-value = CONV string( gv_step ).
    ELSE.
      INSERT VALUE #( name = 'GV_STEP' value = CONV string( gv_step ) ) INTO TABLE ct_values.
    ENDIF.
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~process_input_module.
    IF line_exists( ct_values[ name = 'GV_STEP' ] ).
      gv_step = CONV #( ct_values[ name = 'GV_STEP' ]-value ).
    ENDIF.
    CASE is_context-module.
      WHEN 'USER_COMMAND_0100'.
        CASE is_context-ucomm.
          WHEN 'BACK'.
            io_session->get_dialog( )->leave_to_screen( '0000' ).
        ENDCASE.
      WHEN OTHERS.
        RETURN.
    ENDCASE.
    IF line_exists( ct_values[ name = 'GV_STEP' ] ).
      ct_values[ name = 'GV_STEP' ]-value = CONV string( gv_step ).
    ELSE.
      INSERT VALUE #( name = 'GV_STEP' value = CONV string( gv_step ) ) INTO TABLE ct_values.
    ENDIF.
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~process_on_value_request.
    DATA ct_values TYPE zif_gg_dynpro_types_v1=>ty_values.
    ct_values = it_values.
    rt_values = io_session->get_compatibility( )->get_value_help_values( ).
  ENDMETHOD.

  METHOD zif_gg_screen_provider_v1~process_on_help_request.
    DATA ct_values TYPE zif_gg_dynpro_types_v1=>ty_values.
    ct_values = it_values.
    IF line_exists( ct_values[ name = 'GV_RESULT' ] ).
      rv_text = ct_values[ name = 'GV_RESULT' ]-value.
    ENDIF.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~get_settings.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~top_of_page.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~end_of_page.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~top_of_page_during_line_sel.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~at_line_selection.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~at_user_command.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_list_processing_v1~at_pf.
    RETURN.
  ENDMETHOD.

  METHOD zif_gg_resumable_v1~resume.
* Continuation states are explicit so unsupported suspension semantics remain visible.
    CASE is_resume-continuation-id.
      WHEN 'AFTER_0100'.
        DATA(lo_writer) = io_session->get_list( )->get_writer( ).
        gv_step = 2.
        lo_writer->write_field( VALUE #( text = 'After navigation, step' placement = VALUE #( new_line = abap_true ) ) ).
        lo_writer->write_field( VALUE #( text = |{ gv_step }| ) ).
      WHEN 'AFTER_SUBMIT'.
        DATA(lo_writer) = io_session->get_list( )->get_writer( ).
        gv_step = 2.
        lo_writer->write_field( VALUE #( text = 'After navigation, step' placement = VALUE #( new_line = abap_true ) ) ).
        lo_writer->write_field( VALUE #( text = |{ gv_step }| ) ).
      WHEN OTHERS.
        RETURN.
    ENDCASE.
  ENDMETHOD.

ENDCLASS.
