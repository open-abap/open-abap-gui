REPORT zcomposite_selection_form.
PARAMETERS p_name TYPE c LENGTH 4 DEFAULT 'ABAP'.
DATA lv_text TYPE string.
FORM greet USING iv_name TYPE string CHANGING cv_text TYPE string.
  cv_text = iv_name.
ENDFORM.
START-OF-SELECTION.
PERFORM greet USING p_name CHANGING lv_text.
WRITE lv_text.
